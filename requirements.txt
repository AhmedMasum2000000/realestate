# Runtime dependencies for the provisioning toolkit.
PyYAML>=6.0
requests>=2.31
Jinja2>=3.1            # static site templating for the property catalog
paramiko>=3.4          # SSH/SFTP; without it we shell out to `ssh`/`scp`

# Development only
pytest>=8.0
